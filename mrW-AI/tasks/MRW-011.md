---
id: MRW-011
title: "Update Footer.tsx + Header.tsx — links, tagline, nav"
status: done
size: S
session_group: S5
plan: portfolio-rebrand-ai
dependencies: [MRW-006, MRW-008]
---

## Context

`Footer.tsx` needs the LinkedIn URL corrected, a Medium icon link added, and the tagline updated. `Header.tsx` needs "Work" and "Background" nav anchors added to match the new sections.

## Footer.tsx Changes

**Tagline** (line 17):
```tsx
// Before:
Technical consultant specializing in eInvoicing, AI compliance, and scalable systems.
// After:
AI Transformation Consultant — helping engineering organisations adopt AI, build automation, and govern it responsibly.
```

**LinkedIn URL** (line 33): Ensure it is exactly `https://www.linkedin.com/in/adrian-c-pop/`

**Add Medium link** (after LinkedIn, before GitHub):
```tsx
<a
  href="https://medium.com/@adrian.c.pop"
  target="_blank"
  rel="noopener noreferrer"
  className="group p-3 rounded-xl bg-background/50 hover:bg-primary/10 hover:shadow-glow-modern transition-all duration-300 touch-manipulation"
  aria-label="Medium"
>
  <BookOpen className="w-5 h-5 text-muted-foreground group-hover:text-primary transition-colors" />
</a>
```
Import `BookOpen` from `lucide-react`.

**Footer quick nav** — add two new scroll buttons:
- "Work" → `#work` (CaseStudiesSection)
- "Background" → `#background` (EarlyCareerSection)

## Header.tsx Changes

**Desktop + mobile nav items array** — add:
```tsx
{ href: "#work", label: "Work" },
```
Place between "Services" and "Testimonials".

**"My Work" dropdown** — add a second item:
```tsx
<DropdownMenuItem asChild>
  <Link to="/fiscal-alerts">Fiscal Alerts</Link>
</DropdownMenuItem>
```
(already exists — just ensure "Work" anchor is also in main nav)

## Acceptance Criteria

- [ ] Footer tagline references AI transformation — not eInvoicing
- [ ] LinkedIn URL is `https://www.linkedin.com/in/adrian-c-pop/` (with trailing slash, `www.`)
- [ ] Medium icon link present in footer social links
- [ ] Footer nav includes "Work" and "Background" links
- [ ] Header desktop nav includes "Work" item scrolling to `#work`
- [ ] Header mobile nav includes "Work" item
- [ ] TypeScript compiles without errors
