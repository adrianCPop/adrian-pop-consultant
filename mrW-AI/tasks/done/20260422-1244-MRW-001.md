---
id: MRW-001
title: "Fix merge conflict in backend/server.py"
status: done
size: S
session_group: S1
plan: portfolio-rebrand-ai
dependencies: []
last_runtime: codex
last_updated: 2026-04-22T12:44:30Z
---

## Context

`backend/server.py` had an unresolved git merge conflict. The file could not be
imported and the backend could not start until it was resolved.

The kept implementation uses the clean lifecycle/readiness structure and restores
the required mounted routes and CORS middleware from the conflicting branch.

## Acceptance Criteria

- [x] `backend/server.py` contains no git conflict markers (`<<<<<<<`, `=======`, `>>>>>>>`)
- [x] The kept version uses `on_startup`/`on_shutdown` lifecycle hooks
- [x] `/health` returns `{"status": "ok", ...}` (liveness)
- [x] `/ready` returns `{"status": "ready", ...}` or 503 (readiness)
- [x] `GET /` returns app info
- [x] `POST /api/status` and articles routes are still mounted
- [x] `python -c "import server"` runs without error from the `backend/` directory
- [x] Duplicate `logging.basicConfig` calls removed — only one at module level

## Implementation Notes

- Kept the clean `on_startup` / `on_shutdown` branch of the conflict.
- Restored `CORSMiddleware` setup.
- Restored the `articles_router` mount under `/api`.
- Verified import and route behavior using `.venv` plus `fastapi.testclient.TestClient`.
