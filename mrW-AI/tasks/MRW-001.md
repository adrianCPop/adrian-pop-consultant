---
id: MRW-001
title: "Fix merge conflict in backend/server.py"
status: done
size: S
session_group: S1
plan: portfolio-rebrand-ai
dependencies: []
---

## Context

`backend/server.py` has an unresolved git merge conflict (lines 60–141). The file cannot be imported and the backend will not start until it is resolved. This must be completed before any backend task.

The `>>>>>>> deb71a9 (Sync: server changes)` side is the correct clean version — it has proper lifecycle hooks (`on_startup`, `on_shutdown`), liveness/readiness probes, and clean route definitions. The `HEAD` side duplicates middleware setup and has inconsistent patterns.

## Acceptance Criteria

- [ ] `backend/server.py` contains no git conflict markers (`<<<<<<<`, `=======`, `>>>>>>>`)
- [ ] The kept version uses `on_startup`/`on_shutdown` lifecycle hooks
- [ ] `/health` returns `{"status": "ok", ...}` (liveness)
- [ ] `/ready` returns `{"status": "ready", ...}` or 503 (readiness)
- [ ] `GET /` returns app info
- [ ] `POST /api/status` and articles routes are still mounted
- [ ] `python -c "import server"` runs without error from the `backend/` directory
- [ ] Duplicate `logging.basicConfig` calls removed — only one at module level
